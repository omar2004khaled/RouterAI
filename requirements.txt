# WhatsApp Message Notification Router — Python dependencies
# Install with: pip install -r requirements.txt
# Then run model setup: python setup.py
# Then run pipeline:    python code/main.py

# Core data processing
pandas==2.3.3
numpy==2.3.1
scikit-learn==1.7.0

# Image processing and OCR
Pillow==11.3.0
easyocr==1.7.2

# Deep learning (CPU-only; GPU version also supported)
# On a GPU machine replace with: torch>=2.0.0 (without +cpu suffix)
torch==2.9.1

# Speech-to-text (voice note transcription)
faster-whisper==1.2.1
huggingface-hub==1.15.0
ctranslate2>=4.0.0

# LLM fallback reasoning (optional — pipeline works without an API key)
google-generativeai==0.8.5
